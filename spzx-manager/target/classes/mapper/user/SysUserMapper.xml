<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namesapce对应 mapper接口的全限定符-->
<mapper namespace="com.ptt.spzx.manager.mapper.SysUserMapper">
    <sql id="columns">
        id,username userName ,password,name,phone,avatar,description,status,create_time,update_time,is_deleted
    </sql>
    <resultMap id="sysUser" type="com.ptt.spzx.model.entity.system.SysUser" autoMapping="true"/>
    <select id="selectByUserName" resultType="com.ptt.spzx.model.entity.system.SysUser">
        SELECT <include refid="columns"/> FROM sys_user where username=#{userName} and is_deleted=0;
    </select>
    <select id="selectAll" resultMap="sysUser" >
        SELECT <include refid="columns"/> FROM sys_user
        <where>
            <if test="keyword != null and keyword !='' ">
                username like CONCAT('%',#{keyword},'%')
            </if>
            <if test="createTimeBegin != null and createTimeBegin != ''">
                and create_time >= #{createTimeBegin}
            </if>
            <if test="createTimeEnd != null and createTimeEnd !='' " >
                and create_time &lt;=  #{createTimeEnd}
            </if>
            and is_deleted=0
        </where>
        order by id desc
    </select>
    <insert id="insert">
        insert into sys_user(id,
        username,
        password,
        name,
        phone,
        avatar,
        description,
        status) values(
        #{id},
        #{userName},
        #{password},
        #{name},
        #{phone},
        #{avatar},
        #{description},
        #{status})
    </insert>

    <update id="update">
        update sys_user SET
        <if test="userName != null and userName != ''">
            username = #{userName},
        </if>
        <if test="password != null and password != ''">
            password = #{password},
        </if>
        <if test="name != null and name != ''">
            name = #{name},
        </if>
        <if test="phone != null and phone != ''">
            phone = #{phone},
        </if>
        <if test="avatar != null and avatar != ''">
            avatar = #{avatar},
        </if>
        <if test="description != null and description != ''">
            description = #{description},
        </if>
        <if test="status != null and status != ''">
            status = #{status},
        </if>
        update_time=now()
        where id=#{id}
    </update>
    <update id="deleteById">
        UPDATE sys_user SET is_deleted=1,update_time=now() where id=#{id}
    </update>

</mapper>