<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namesapce对应 mapper接口的全限定符-->
<mapper namespace="com.ptt.spzx.manager.mapper.CategoryBrandMapper">
    <resultMap id="CategoryBrandMap" type="com.ptt.spzx.model.entity.product.CategoryBrand" autoMapping="true"/>

    <sql id="columns">
        id,brand_id,category_id,create_time,update_time,is_deleted
    </sql>
    <select id="selectByBrandCategory" resultMap="CategoryBrandMap">

        select cb.id,cb.brand_id ,cb.category_id,cb.create_time,cb.update_time,
        c.name categoryName,b.name brandName,b.logo
        from category_brand cb
        left join category c on c.id = cb.category_id
        left join brand b on b.id=cb.brand_id
        <where>
            <if test="brandId != null and brandId!='' ">
                and cb.brand_id=#{brandId}
            </if>
            <if test="categoryId != null and categoryId != '' ">
                and cb.category_id=#{categoryId}
            </if>
            and cb.is_deleted = 0
        </where>
        order by cb.id desc
    </select>

    <insert id="insert">
        insert into category_brand(brand_id,category_id) values(#{brandId},#{categoryId})
    </insert>

    <update id="updateById">
        update category_brand set
        <if test="brandId != null and brandId != ''">
            brand_id = #{brandId},
        </if>
        <if test="categoryId != null and categoryId != ''">
            category_id = #{categoryId},
        </if>
        update_time =  now()
        where
        id = #{id}
    </update>
    <update id="deleteById">
        update category_brand set update_time = now() ,is_deleted=1 where id = #{id}
    </update>
    <select id="selectBrandByCategoryId" resultMap="com.ptt.spzx.manager.mapper.BrandMapper.brandMap">
        select b.* from brand b
        inner join category_brand cb on cb.brand_id=b.id and cb.category_id=#{categoryId} and cb.is_deleted = 0
        order by b.id desc

    </select>


</mapper>