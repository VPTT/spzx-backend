<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namesapce对应 mapper接口的全限定符-->
<mapper namespace="com.ptt.spzx.manager.mapper.CategoryMapper">
    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,name,image_url,parent_id,status,order_num,create_time,update_time,is_deleted
    </sql>
    <resultMap id="categoryMap" type="com.ptt.spzx.model.entity.product.Category" autoMapping="true">
    </resultMap>
    <select id="selectByParentId" resultMap="categoryMap">
        select <include refid="columns"/> from category where parent_id=#{id} and is_deleted = 0
        order by id desc
    </select>

    <select id="selectAll" resultMap="categoryMap">
        select <include refid="columns"/> from category where is_deleted = 0
        order by id desc
    </select>

    <insert id="insertBatch" >
        insert into category (
        id,
        name,
        image_url,
        parent_id,
        status,
        order_num,
        create_time ,
        update_time ,
        is_deleted
        ) values
        <foreach collection="cachedDataList" item="data" separator=",">
            (#{data.id},
            #{data.name},
            #{data.imageUrl},
            #{data.parentId},
            #{data.status},
            #{data.orderNum},
            now(),
            now(),
            0)
        </foreach>

    </insert>


</mapper>