<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namesapce对应 mapper接口的全限定符-->
<mapper namespace="com.ptt.spzx.manager.mapper.SysRoleMapper">
    <!--  映射查询到的字段 -->
    <resultMap id="sysRoleMap" type="com.ptt.spzx.model.entity.system.SysRole" autoMapping="true"></resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,role_name,role_code,description,create_time,update_time,is_deleted
    </sql>

    <select id="findAll" resultMap="sysRoleMap">
        SELECT <include refid="columns"/> FROM sys_role WHERE is_deleted=0
    </select>


    <select id="findMyPage" resultMap="sysRoleMap" >
        SELECT <include refid="columns"/> FROM sys_role
        <where>

            <if test="roleName !=null and roleName != '' ">
                AND role_name like CONCAT('%',#{roleName},'%')
            </if>
            and is_deleted=0
        </where>
        order by id desc
    </select>
    <insert id="save">
        INSERT INTO sys_role(id,role_name,role_code,description) VALUES (#{id},#{roleName},#{roleCode},#{description});
    </insert>
    <update id="update">
        UPDATE sys_role SET
        <if test="roleName !=null and roleName !='' ">
            role_name= #{roleName},
        </if>
        <if test="roleCode !=null and roleCode !=''">
            role_code =#{roleCode},
        </if>
        <if test="description != null and description != ''">
            description = #{description},
        </if>
        update_time= now()
        where id=#{id};
    </update>

    <!--逻辑删除-->
    <update id="deleteById">
        UPDATE sys_role SET is_deleted=1 where id=#{id}
    </update>


    <!--
    <sql id="findPageWhere">
        <where>
            <if test="roleName != null and roleName != ''">
                and role_name like CONCAT('%',#{roleName},'%')
            </if>
            and is_deleted = 0
        </where>
    </sql>

    <select id="findMyPage" resultMap="sysRoleMap">
        select <include refid="columns" />
        from sys_role
        <include refid="findPageWhere"/>
        order by id desc
    </select>
    -->

</mapper>