<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namesapce对应 mapper接口的全限定符-->
<mapper namespace="com.ptt.spzx.manager.mapper.SysMenuMapper">
    <resultMap id="sysMenuMap" type="com.ptt.spzx.model.entity.system.SysMenu" autoMapping="true"/>
    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,parent_id,title,component,sort_value,status,create_time,update_time,is_deleted
    </sql>
    <select id="findAll" resultMap="sysMenuMap">
        SELECT <include refid="columns"/> FROM sys_menu WHERE is_deleted=0 order by sort_value
    </select>
    <insert id="insert">
        INSERT INTO sys_menu(
        parent_id,
        title,
        component,
        sort_value,
        status)
        VALUES(
        #{parentId},
        #{title},
        #{component},
        #{sortValue},
        #{status})
    </insert>

    <update id="update">
        UPDATE sys_menu SET
        <if test="parentId != null and parentId !='' ">
            parent_id=#{parentId},
        </if>
        <if test="title != null and title != ''">
            title = #{title},
        </if>
        <if test="component != null and component != ''">
            component = #{component},
        </if>
        <if test="sortValue != null">
            sort_value = #{sortValue},
        </if>
        <if test="status != null">
            status = #{status},
        </if>
        update_time=NOW()
        where
        id = #{id}

    </update>

    <update id="delete">
        UPDATE sys_menu SET update_time = now(), is_deleted=1 where id=#{id}
    </update>
    <select id="countChildren" resultType="integer">
        SELECT count(*) FROM sys_menu where parent_id=#{id} and is_deleted = 0
    </select>

    <select id="findMenuByUserId" resultMap="sysMenuMap">
        select distinct m.* FROM sys_menu m
        inner join sys_role_menu rm On m.`id`=rm.`menu_id`
        inner join sys_user_role ur On ur.`role_id`=rm.`role_id`
        where ur.`user_id`=#{id} and m.is_deleted=0
    </select>

    <select id="selectById" resultMap="sysMenuMap">
        SELECT <include refid="columns"/> FROM sys_menu where id=#{id}
    </select>
</mapper>